package com.assignment.flow.extblocker.repository;

import com.assignment.flow.extblocker.entity.DefaultExt;
import org.springframework.data.repository.CrudRepository;

public interface DataRepository extends CrudRepository<DefaultExt, Long> {
}
